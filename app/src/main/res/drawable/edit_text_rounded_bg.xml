<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="40dp"/> <!-- Ajusta este valor para cambiar el radio de las esquinas -->
    <solid android:color="@color/blanco_opaco"/> <!-- Cambia este color al color deseado -->
    <stroke
        android:width="3dp"
        android:color="@color/blanco_opaco"/>

</shape>
