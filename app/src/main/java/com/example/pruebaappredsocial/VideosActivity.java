package com.example.pruebaappredsocial;

import androidx.appcompat.app.AppCompatActivity;

public class VideosActivity extends AppCompatActivity {
/*
    private RecyclerView recyclerView;
    private VideoAdapter videoAdapter;
    private List<Video> videoList;

    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_videos);

        // Inicializa el RecyclerView
        recyclerView = findViewById(R.id.recyclerViewVideos);
        recyclerView.setLayoutManager(new LinearLayoutManager(this));

        // Inicializa la lista de videos
        videoList = new ArrayList<>();

        // Añade videos de ejemplo a la lista
        videoList.add(new Video("Fulanito Garcia", "Mi video genial!", "url_de_miniatura_1", 2300, 4500, 6700));
        videoList.add(new Video("María Lopez", "¡Increíble paisaje!", "url_de_miniatura_2", 1500, 2500, 3400));
        videoList.add(new Video("Carlos Ruiz", "Tutorial de programación", "url_de_miniatura_3", 1200, 3400, 2800));

        // Configura el adaptador del RecyclerView
        videoAdapter = new VideoAdapter(this, videoList);
        recyclerView.setAdapter(videoAdapter);
    }
}


 */
}
